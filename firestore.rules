rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write access for development purposes.
    // CAUTION: This is insecure and should be updated before production use.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
